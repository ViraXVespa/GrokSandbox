 // ... existing content ...

## Settings Sync (In Progress)

Basic foundation added:
- `SETTINGS_REQUEST` and `CONFIG_UPDATE` message types
- Methods on ConnectionManager to request/apply config
- Handler stubs in RemoteSessionManager

Full two-way live sync is planned for the mobile settings UI.